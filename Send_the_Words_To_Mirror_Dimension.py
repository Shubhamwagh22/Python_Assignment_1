# Que_no_1 : Write a Python program that accepts a word from the user and reverse it?

word = str(input("Enter the word : "))
print ("Input : ", word)

name = " "
for i in word:
    name = i + name

print ("Output : ", name)
